// import {Pipe, PipeTransform} from '@angular/core';
// import {ILeftMenu} from "../interfaces/left-menu.interfaces";
//
// @Pipe({
// 	name: 'searchPipe'
// })
// export class SearchPipe implements PipeTransform {
//
// 	transform(list, inputVel: string): ILeftMenu[] {
//     if (!inputVel && !inputVel.trim().length) {
//       return list
//     }
//     return list.filter(res => res.elm_name.trim().toLocaleUpperCase().includes(inputVel.trim().toLocaleUpperCase()))
//   }
// }
